import React from "react";
import PropTypes from "prop-types";

const Comments = () => {
    return (
        null
    );
}

Comments.propTypes = {
    user: PropTypes.object
};
 
export default Comments;